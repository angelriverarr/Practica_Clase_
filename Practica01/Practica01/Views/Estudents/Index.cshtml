@model List<Practica01.Models.Estudiantes>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

  
    <a class="btn btn-success" href="@Url.Action("Guardar","Estudents")">Add</a>
    @Html.ActionLink("Save","Guardar","Estudents",new{@class="btn btn-danger"})

<table  class="table table-dark table-striped" id= "tablaEst">
    <thead>
        <tr>
             
            <th>Name</th>
            <th>Last Name</th>
            <th>Acciones</th>
            
        </tr>
    </thead>
    <tbody>
        @foreach(var iterar in Model)
        {
            <tr>
                
                <td>@iterar.name </td>
                <td>@iterar.lastName </td>
                <td>@Html.ActionLink("Delete","Delete",new{id=@iterar.id},new{@class="btn btn-danger"} )
                 @Html.ActionLink("Update","Load",new{id=@iterar.id},new{@class="btn btn-warning"} )</td>

            </tr>
        }
    </tbody>

</table>

@section scripts{
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function(){
        alert ('ready load');
        

        var tabla = $('#tablaEst').DataTable();

        }
        );

    </script>
}